PORT=3000
JWT_SECRET=knowgap_ai_secret_key_2024_very_secure

# Cloud MongoDB (Atlas) — use your own cluster URI
MONGO_URI=mongodb+srv://USER:PASSWORD@cluster0.example.mongodb.net/knowgap?retryWrites=true&w=majority

# If Atlas is unreachable, the app auto-tries local MongoDB at:
# mongodb://127.0.0.1:27017/knowgap
