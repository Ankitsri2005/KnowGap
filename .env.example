PORT=3000
JWT_SECRET=change_this_to_a_long_random_secret

# Required on Render. For local dev, paste the same Atlas URI from Render → Environment.
# Leave empty to try mongodb://127.0.0.1:27017/knowgap (needs MongoDB installed locally).
MONGO_URI=
